apiVersion: v1
kind: Service
metadata:
  name: flask-api
  namespace: flask-app
spec:
  type: NodePort
  selector:
    app: flask-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
