apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flask-api
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - flask-app
  selector:
    matchLabels:
      app: flask-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
